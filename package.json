{
  "name": "nanoalpha",
  "version": "0.1.0",
  "private": true,
  "description": "Browser tool and public API for extracting transparent images",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.0",
    "fastify": "^5.0.0",
    "sharp": "^0.35.4"
  }
}
